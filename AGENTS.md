# Agent Rules - Farming Project

## Remote Repository
- **Remote URL**: `https://github.com/Nitheesh2007/vidhyakartechno.git`
- **Target Branch**: `main`

## Automatic Git Commits & Pushes
- After making code or configuration edits to files in this repository, automatically stage (`git add .`), commit (`git commit -m "..."`), and push (`git push origin main` or to `https://github.com/Nitheesh2007/vidhyakartechno.git`) the changes.
- Always ensure the local branch is synchronized with the remote (`git pull --rebase origin main`) prior to pushing.
